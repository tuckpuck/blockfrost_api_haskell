To get this project working, add a [Blockfrost API key](https://blockfrost.io/) to the src/.Env file. Then remove the . before the .Env filename.

Run main program with `cabal run main`
Run REPL with `cabal repl`
